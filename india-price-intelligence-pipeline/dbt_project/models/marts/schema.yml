version: 2

models:
  - name: mart_price_trends
    columns:
      - name: week
        tests: [not_null]
      - name: canonical_commodity
        tests: [not_null]

  - name: mart_margin_by_commodity
    columns:
      - name: canonical_commodity
        tests: [not_null]

  - name: mart_state_wholesale_comparison
    columns:
      - name: state
        tests: [not_null]

  - name: mart_weekly_summary
    columns:
      - name: week
        tests: [not_null]

  - name: mart_pipeline_health
    columns:
      - name: run_date
        tests: [not_null]
